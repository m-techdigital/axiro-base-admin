import {createContext,useContext,useEffect,useState} from 'react'; import auth from '../modules/auth/service'; import {getAccessToken,clearAuth} from '../utils/auth'; const C=createContext(null); export function AuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true);const fetchMe=async()=>{try{const r=await auth.me();setUser(r.data)}catch{clearAuth();setUser(null)}finally{setLoading(false)}};useEffect(()=>{getAccessToken()?fetchMe():setLoading(false)},[]);const logout=async()=>{try{await auth.logout()}finally{clearAuth();setUser(null)}};return <C.Provider value={{user,setUser,loading,fetchMe,logout}}>{children}</C.Provider>} export const useAuth=()=>useContext(C);