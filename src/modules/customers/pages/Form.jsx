import {Button,Card,Form,Input,Select,message} from 'antd';import {useEffect,useState} from 'react';import {useNavigate,useParams} from 'react-router-dom';import PageHeader from '../../../components/base/PageHeader';import service from '../service';
export default function CustomerForm(){const{id}=useParams(),n=useNavigate(),[f]=Form.useForm(),[loading,setLoading]=useState(false);useEffect(()=>{if(id)service.get(id).then(r=>f.setFieldsValue(r.data))},[id,f]);const save=async v=>{setLoading(true);try{id?await service.update(id,v):await service.create(v);message.success('Đã lưu');n('/customers')}catch(e){if(e.errors)f.setFields(Object.entries(e.errors).map(([name,errors])=>({name,errors})));message.error(e.message)}finally{setLoading(false)}};return <div className='page'><PageHeader title={id?'Cập nhật khách hàng':'Tạo khách hàng'}/><Card className='form-card'><Form form={f} layout='vertical' onFinish={save} initialValues={{status:'active'}}><Form.Item name='username' label='Tên đăng nhập' rules={[{required:true}]}><Input/></Form.Item><Form.Item name='name' label='Tên khách hàng' rules={[{required:true}]}><Input/></Form.Item><Form.Item name='email' label='Email'><Input/></Form.Item><Form.Item name='phone' label='Điện thoại'><Input/></Form.Item><Form.Item name='password' label={id?'Mật khẩu mới (để trống nếu không đổi)':'Mật khẩu'} rules={id?[]:[{required:true},{min:8}]}><Input.Password/></Form.Item><Form.Item name='status' label='Trạng thái'><Select options={['active','inactive','blocked'].map(v=>({value:v,label:v}))}/></Form.Item><Button type='primary' htmlType='submit' loading={loading}>Lưu</Button> <Button onClick={()=>n('/customers')}>Hủy</Button></Form></Card></div>}
