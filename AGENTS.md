# AGENTS
Keep modules in `src/modules/<module>`, reuse `createCrudService`, `useList`, `useDetail`, `useRelationOptions`, and base components. Do not introduce RBAC/company/project dependencies. Preserve 401 refresh and 422 field error handling.
