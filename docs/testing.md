# Testing
Run the Admin gates that match the changed surface:

- `npm run format`
- `npm run check:all`
- `npm run lint`
- `npm run build`

For parent/base work, keep `check:parent-foundation`, `check:base-ownership`, `check:parent-base-parity`, `check:parent-view-foundation` and `check:semantic-owners` green.
